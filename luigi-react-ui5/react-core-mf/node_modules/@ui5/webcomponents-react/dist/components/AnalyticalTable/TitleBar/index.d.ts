import React, { ReactNode } from 'react';
export interface TitleBarProps {
    children: ReactNode;
    titleBarId: string;
}
export declare const TitleBar: React.ForwardRefExoticComponent<TitleBarProps & React.RefAttributes<HTMLDivElement>>;
